import 'package:flutter/material.dart';

class EmergencyUnlockScreen extends StatelessWidget {
  const EmergencyUnlockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Emergency Unlock Screen')));
  }
}
